# Resolution Plan — Remaining Work Only (Updated 2026-09-13)

Based on `webgpu_notes/TASKS.md` and the previous versions of this doc. **Everything already verified `DONE` has been removed.** Tiers are ordered easiest → hardest; items within each tier are also ordered easiest → hardest.

**Repo state**: HEAD `5d4358927` on `sync/4.7-stable` ("disabled noise unhelpful warnings for benign behaviours"). `servers/rendering/rendering_device_commons.h`/`servers/rendering/renderer_rd/shader_rd.{h,cpp}`/`servers/rendering/rendering_device.cpp`/`editor/export/shader_baker_export_plugin.cpp`/`drivers/webgpu/rendering_device_driver_webgpu.cpp`/`drivers/webgpu/wgsl_precompile.py`/`webgpu_tests/shader_corpus/expected_failures.json` have uncommitted changes on top of that (this session's fixes, below). Tiers 1–2 and Tier 3 #14/#15 from earlier versions of this doc are done and committed — not repeated here.

## Fixed this session (all uncommitted)

- **Task 7.11 (swap chain format hardcoded to BGRA8Unorm)** — a fresh `errors-chrome.txt` capture surfaced `WebGPU canvas configured with a different format than is preferred by this device ("rgba8unorm")` as live, not theoretical. `swap_chain_create()` now queries `wgpuSurfaceGetCapabilities()` for the browser's real preferred format instead of hardcoding BGRA8Unorm. Task 7.9's alpha-strip logic (which relied on the swap chain always being BGRA8Unorm) was updated alongside it to key off `rp->is_swap_chain_pass` instead of format equality. Object-file compile-checked clean (both `template_release`/`template_debug`), `driver_unit_tests` 327/327.
- **Task 8.10's stale status line** — closed out (doc bookkeeping only; Phase 9's rounds 15-37 already ran the user's real project dozens of times with no recurrence).
- **Task 7.13's audit note (`taa.cpp`/`motion_vectors_store.cpp`)** — checked, no fix needed. Both bind their depth texture the same way Bokeh DOF did; manually traced the resulting WGSL through every gate of `_reclassify_single_component_depth_textures()` and confirmed both are already correctly handled.
- **`modules/SCsub` debug_features build staleness (Task 9.5 Round 15)** — fixed. Added `env.Depends(lib, env.Value(env.module_list))` on the `modules` library target, so the compiled object's rebuild decision no longer depends solely on implicit `#include`-scanning of a generated header (which proved unreliable across a `template_debug`↔`template_release` switch). **Verified empirically, not just by code review**: built `template_debug` → `template_release` → `template_debug` → `template_release` in sequence in this same tree and checked `nm` on the compiled object each time — `initialize_objectdb_profiler_module`/`uninitialize_objectdb_profiler_module` correctly present in every debug build and correctly absent in every release build, with SCons still skipping unnecessary recompiles when nothing changed.
- **6 remaining `wgsl_precompile.py` GLSL-level failures** — all fixed by tracing each real C++ caller for the exact define it always passes: `octmap_downsampler`/`octmap_filter`/`octmap_roughness` needed `OCTMAP_FORMAT` (registry now covers both real values, `rgb10_a2`/`rgba16f`); `ssao_blur`/`ssil_blur` needed one of `MODE_NON_SMART`/`MODE_SMART`/`MODE_WIDE` (registry now covers all three); `subsurface_scattering` needed one of `USE_11_SAMPLES`/`USE_17_SAMPLES`/`USE_25_SAMPLES` (registry now covers all three). `giprobe_write.glsl` was removed from the registry rather than fixed — confirmed dead code (nothing in the engine references it) that also hard-fails glslang regardless of any define (`output` is a genuine GLSL reserved word used as a block instance name). Combined with the earlier `taa_resolve` fix, `wgsl_precompile.py` now reports **195 compiled, 0 tint failures** — the only remaining GLSL-level failure is `cluster_render.glsl` (Task 9.4's already-tracked, separately-scoped subgroup gap). `shader_corpus` 13/13, `preprocessing_tests` 192/192 (+1 skip), no regressions.

**Side-finding, not investigated**: 3 pre-existing `SKIP: ... (no stage)` registry entries noticed during the above (`cube_to_octmap.glsl`, `cluster_debug.glsl` ×2) — a different, smaller failure mode (registry references a stage the file doesn't define) than any of the above. Worth a quick follow-up but out of scope here.

- **"code is unreachable" console warnings (Task 9.8)** — root-caused instead of re-dismissing as cosmetic. Any GLSL helper whose `if`/`else` both `return` (recurs across `ssao.glsl`, `ssil.glsl`, `voxel_gi_debug.glsl`, `sdfgi_direct_light.glsl`, `gi.glsl`, `scene_forward_clustered.glsl`, `canvas.glsl`) makes Tint's WGSL writer emit a synthetic, provably-dead trailing `return <T>();` (a Tint code-generation choice, not a real dead-SPIR-V-block — confirmed by testing SPIRV-Tools' own `CreateDeadBranchElimPass()`/`CreateBlockMergePass()` against it and seeing no change, so no preprocessing pass can remove it). Fixed properly: set `tint::wgsl::writer::Options::disable_unreachable_code_warning = true` in `tint_wrapper.cpp`, the same `diagnostic(off, ...)`-directive mechanism already used for the two existing `allow_non_uniform_*` relaxations. Verified the directive now appears in real WGSL output for both `ssao.glsl` and `gi.glsl`; full local suite clean (`shader_corpus` 13/13, `preprocessing_tests` 192/192+1 skip, `driver_unit_tests` 327/327).
- **Task 9.3 (Forward+ capability audit) — fully closed out.** Diffed decal (`decal_atlas_srgb`, plain `texture2D`) and reflection-probe (`reflection_atlas`, `texture2DArray`) bindings and sampling code line-for-line between `forward_mobile` and `forward_clustered`: byte-identical types and binding indices, `reflection_process()` itself lives in a file shared by both. No `samplerCubeArray`/`textureCubeArray` anywhere — reflection probes use a flat 2D array atlas, not cube arrays. For indirect draws: the only two `draw_list_draw_indirect` call sites in the whole `renderer_rd/` tree are Mobile's and Clustered's multimesh paths, both issuing a hardcoded `draw_count=1`; no other GPU-driven multi-draw call site exists anywhere in the renderer. The driver's indirect-draw implementation (loop of individual `wgpuRenderPassEncoderDrawIndexedIndirect()` calls, since WebGPU has no true multi-draw-indirect) already handles this correctly for both renderers today. **Conclusion: no Clustered-specific capability gap remains** — the only real Forward+ risk left is Task 9.4's already-separately-tracked subgroup precompile gap, plus the dormant multiview `ViewIndex` crash (VR-only, out of scope per the driver's own `SUPPORTS_MULTIVIEW: false`).

**Live-verified**: rebuilt the `template_release` web template from committed HEAD, exported `webgpu_tests/test_project`'s ShaderCoverage scene (checksum-confirmed fresh), and captured the full browser console via Playwright on real GPU hardware (RTX 4080 SUPER). Confirmed the running build's version string matches the fix commit exactly, and confirmed **zero** occurrences of either the swap-chain-format warning or the "code is unreachable" warning across a full 10-frame render exercising SSAO/SSIL/SDFGI/decals/reflection-probes/FSR2/TAA — only unrelated, pre-existing cosmetic warnings remained. Task 9.3's Forward+ audit is still static-analysis-only (no live Clustered-path render attempted, since the ShaderCoverage scene doesn't isolate Clustered vs. Mobile specifically) — worth a live Clustered run if that ever becomes a priority, but not blocking.
- **`GODOT_DUMP_SPIRV`/native-editor SPIR-V version mismatch (Task 9.9) — fixed at the source, not the driver.** First evaluated (per user request) whether migrating the WebGPU driver's own target from SPIR-V 1.3 to 1.4 (matching Vulkan's default) would be the better long-term fix — concluded no: this exact scenario already happened three times by accident (Tasks 8.6, 8.8, 9.7), each producing 113–260 generic failures when 1.4 SPIR-V hit the 1.3-tuned preprocessing pipeline, for zero functional upside (1.4 unlocks nothing WebGPU needs). Fixed the actual mismatch instead: `ShaderRD::compile_stages()` now recompiles a second copy of each shader stage forced to SPIR-V 1.3/Vulkan-1.1 (the WebGPU driver's exact target) purely for the `GODOT_DUMP_SPIRV` dump, whenever the active editor driver (always non-WebGPU on a native build) isn't already at 1.3 — stored in a new additive `ShaderStageSPIRVData::dump_override_spirv` field, preferred by the existing dump-writer over the driver-native bytes. Guarded to compile out of export templates entirely (`GODOT_DUMP_SPIRV` is editor-only). **Live-verified**: all 204 dumped shaders now genuinely SPIR-V 1.3 (confirmed via header parse, previously all 204 were 1.4); `validate_spirv_dump.mjs` now reports **201/204 passing (98.5%)**, up from 113/204 (55%) — both remaining failures are real, already-documented Vulkan-only variants (`TonemapMobileShaderRD`'s subpass variant, `VrsShaderRD`'s multiview `ViewIndex`), not tooling artifacts. `expected_failures.json` regenerated via `--update-baseline`, shrinking from its stale 31-entry 2026-05-05 version to these 3 real ones; re-run confirms 0 regressions. Full local suite clean (`shader_corpus` 13/13, `preprocessing_tests` 192/192+1 skip, `driver_unit_tests` 327/327); compile-checked clean across native editor, `template_release`, and `template_debug`.
- **Remaining Phase-7 hardening TODOs (7.7/7.10/7.12/7.14) — closed out.** 7.7 (texture bpp hardcoded to 4) and 7.12 (sampler filter validation) turned out to already be correctly fixed by an earlier, undocumented pass — just stale TASKS.md entries, reconciled with no code change. 7.14 (sync-scope heuristic) audited against the current code (which has evolved past the doc's stale line numbers) and found already adequately handled: `command_begin_render_pass()`'s proactive encoder-split check already keys on real usage bits, not merely "is an attachment" as the old writeup claimed, and is conservative by construction (can over-split, never under-split). **7.10 (16-bit Unorm/Snorm → Float remapping) was a genuine, live, previously-mischaracterized bug**: confirmed no emdawnwebgpu upgrade path exists (checked emsdk 4.0.11's `webgpu.h` directly — no native 16-bit-norm format or feature exists yet), but found the Float-format substitute was a raw bit-pattern reinterpretation (not a real value conversion) that `texture_get_usages_supported_by_format()` was silently reporting as fully supported — bypassing Godot's own correct Float32 fallback in `texture_storage.cpp` and producing garbage for any 16-bit grayscale/RG16/RGBA16 image import. Fixed by reporting these 6 formats unsupported, letting the existing engine-level fallback do the (correct) conversion instead. Compile-checked clean on both web targets; `driver_unit_tests` 327/327.
- **`wgsl_precompile.py` subgroup precompile gap (Task 9.4) — fixed, not just patched around.** Added `--target-env vulkan1.1` to the precompiler's `glslangValidator` invocation, matching the runtime driver's actual SPIR-V 1.3 target exactly (previously bare `-V` defaulted to 1.0) — this fixes `cluster_render.glsl`'s subgroup-op compile failure *and* closes a latent, previously-flagged-but-unresolved question about whether the ahead-of-time precompiled cache and the runtime fallback path were silently compiling structurally different SPIR-V (legacy `Uniform`+`BufferBlock` SSBO encoding vs. modern `StorageBuffer`) for the same shaders — confirmed via a hand-compiled diff (`spirv-dis`) that they were, and that at least one preprocessing pass (`strip_writeonly_storage_decoration`) only recognized the modern encoding. **Verified**: standalone precompile run 196/199/0 (up from 195/199/0, `cluster_render.glsl` now included, no new failures); full web builds (`template_release`+`template_debug`) clean end-to-end; `shader_corpus` 13/13, `preprocessing_tests` 192/192+1 skip, `driver_unit_tests` 327/327 all unaffected; **live-verified on real Vulkan-backed WebGPU** — all 8 in-repo `benchmark_*` scenes (sprites/PBR/instancing/particles/animation/postfx/shadows/batching) pass via Playwright with 0 GPU errors and 0 shader errors.

---

## Tier 1 — Small to medium engineering (hours to days)

| # | Task | What to do | Effort |
|---|------|------------|--------|
| 1 | CI for prebuilt verification | No evidence this exists. Add a CI job that rebuilds `tint_convert_cli` and diffs `wgsl_precompile.py` output for reproducibility. Worth bundling `resource_lifecycle`/`screenshot_comparison` into the same effort since both need Playwright, which nothing in this repo's CI currently installs. | 1–2 days |

Remaining Phase-7 hardening TODOs (7.7/7.10/7.12/7.14) and the `wgsl_precompile.py` subgroup precompile gap (Task 9.4) — both formerly listed here — are done; see "Fixed this session" above. **7.8** (buffer_map returns stale/zero data) is a fundamental single-threaded-WASM limitation, not a real fix candidate. **7.15**/**7.17**/**7.18** (temp_views leak, shader module cleanup, WGSL string-remap fragility) fold into Tier 2 item 3 below.

---

## Tier 2 — Major features & hard problems (multi-day to multi-week)

| # | Task | What to do | Effort | Notes |
|---|------|------------|--------|-------|
| 2 | Skeleton atlas defragmentation | Bump allocator has no reclaim path for long-running dynamic scenes (`webgpu_notes/perf_optimization_may3_2026.md`). | 2–4 days | Smallest Tier 2 item — do this before the open-ended investigations below if a quick structural win is wanted. |
| 3 | FSR2 upscaling re-enablement | The buffer-atomic fallback for FSR2's SPD counter is fully implemented and independently verified correct (`NO_IMAGE_ATOMICS` pattern, mirroring `fog.cpp`/SDFGI). FSR2 stays disabled purely because of a separate Tint internal-compiler-error triggered only once a real atomic is present anywhere in FSR2's ~12,000-line preprocessed luminance-pyramid shader (a minimal standalone repro of the same atomic pattern compiles fine — this is size/complexity-triggered). This is now a Tint-bug bisection task, not a fallback-design task. | 3–7 days | |
| 4 | Linux web-export browser compatibility | README lists Linux browsers as TODO — this is browser-side export compatibility, not a native Linux build (which already works, it's how everything above was tested). | 2–5 days | |
| 5 | Windows web-export browser compatibility | Same as above for Windows. Verify scope before starting — check whether `tint_cli`/native builds matter at all here, or whether it's purely browser/JS-shell parity work, before assuming an MSVC build is needed. | 3–7 days | |
| 6 | **SDFGI brightness runaway — the single active blocker, top priority regardless of tier** | 37 rounds in (Task 9.5, item 11). Round 36's cascade-array-binding-collapse fix is committed and WGSL-verified correct but **does not fix the runaway** (Round 37 disproved the apparent confirmation — it was a false positive caused by this scratch project's cascade count coincidentally hitting `gi.cpp`'s white-texture padding). The one remaining, unrefuted lead from Rounds 31-35: `sdfgi_integrate.glsl`'s hand-rolled non-atomic SH rolling-average accumulation (~L330-346) overshoots its own theoretical ceiling by ~481× on WebGPU while staying bit-exact flat on Vulkan over a 300s/51,300-frame capture. TASKS.md's own suggested next step: instrument `cascade.light_data` immediately after `update_light()`'s dispatch on both backends (not just the final `lightprobe_data`) to see whether per-voxel `dst_light` is already diverging before SH accumulation — this would pin the site to `sdfgi_direct_light.glsl`'s bounce-feedback read/write cycle rather than `sdfgi_integrate.glsl` itself. | 1–2 weeks | Do this before anything else in this tier — every other item here is independent of it, and TASKS.md itself calls this the active blocker for any new public build. |
| 7 | Move WGSL string patching to AST-level transforms | Long-term maintainability win for the memcpy/string-remap fragility underlying Task 7.18 above — no shortcuts identified. | 1–2 weeks | |
| 8 | Pre-compile WGSL at export time (full coverage) | Extend `wgsl_precompile.py` to cover every reachable shader variant ahead of time, closing the remaining runtime-Tint-fallback gap entirely (Task 9.4's fix above is a prerequisite, now done). | 2–4 weeks | Largest item in the plan — do last. |

---

## Suggested order of attack

1. **Tier 1** (item 1, CI for prebuilt verification) — 1-2 days.
2. **Ongoing, top priority regardless of tier**: item 6 (SDFGI runaway) — start this in parallel with Tier 1 rather than waiting, since it's the only thing actually blocking a public build.
3. **Tier 2** (items 2–8) after the SDFGI runaway lands — item 2 (skeleton atlas) is the cheapest standalone win; 3 (FSR2) is next-most-tractable since its design work is already done; 4/5 (export browser compat) before the two open-ended, multi-week items (7, 8).
